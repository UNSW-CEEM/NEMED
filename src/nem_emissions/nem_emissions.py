def fetch_total_emissions(x=100, y="abc"):
    """Retrieves NEM emissions data, calculated on dispatch

    Parameters
    ----------
    x : int, optional
        _description_, by default 100
    y : str, optional
        _description_, by default "abc"
    """

    return 0
